<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/mainColor</item>    <!--statusBar 的颜色-->
        <item name="colorAccent">@color/colorAccent</item>
        <!--<item name="android:statusBarColor">@android:color/black</item>--> <!--（5.0）这也可以修改statusBar的颜色，优先级更高-->

        <!--全局的Activity切换时的过渡动画style-->
        <item name="android:windowAnimationStyle">@style/AnimationActivity</item>
    </style>

    <style name="AnimationActivity" parent="android:Animation.Activity">
        <!--打开Activity时，进入的Activity所执行的动画-->
        <item name="android:activityOpenEnterAnimation">@anim/open_enter</item>
        <!--打开Activity时，退出的Activity所执行的动画-->
        <item name="android:activityOpenExitAnimation">@anim/open_exit</item>
        <!--退出Activity时，消失的Activity所执行的动画-->
        <item name="android:activityCloseExitAnimation">@anim/close_exit</item>
        <!--退出Activity时，重新显示的Activity所执行的动画-->
        <item name="android:activityCloseEnterAnimation">@anim/close_enter</item>
    </style>

    <!--可全局使用的分割线-->
    <style name="line">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">1dp</item>
        <item name="android:background">@color/lineColor</item>
        <item name="android:layout_margin">@dimen/lineMargin</item>
    </style>

    <!--全局登录按钮的样式-->
    <style name="commitBtn">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">@dimen/btnHeight</item>
        <item name="android:textSize">@dimen/hintSize</item>
        <item name="android:textColor">@color/white</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_marginLeft">@dimen/marginSize</item>
        <item name="android:layout_marginRight">@dimen/marginSize</item>
        <item name="android:background">@drawable/btn_bg</item>
    </style>

</resources>
